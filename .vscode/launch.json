{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "npm: start"
    }
  ]
}
